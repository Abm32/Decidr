{
  "enabled": true,
  "name": "Run Tests After Task",
  "description": "Runs the test suite after each agent task execution completes",
  "version": "1",
  "when": {
    "type": "agentStop"
  },
  "then": {
    "type": "runCommand",
    "command": "npx vitest --run 2>&1 | tail -50"
  }
}